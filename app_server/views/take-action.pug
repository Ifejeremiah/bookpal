extends layout 

block content 
  .con-showcase
    h1.header #{bookTitle}
    .showcase 
      .options
        .links
          a(href=`/actions/books/update/${bookId}`) 
            .btn Update
          a(href=`/actions/books/delete/${bookId}`)
            .btn.danger Delete
